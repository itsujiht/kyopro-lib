#include <iostream>
#include <math.h>
#include <algorithm>
#include <vector>
#include <string>
#include <numeric>
#include <iomanip>
#include <set>
#include <tuple>
#include <map>
#include <queue>
#include <deque>
#include <functional>
#define ll long long
#define vi vector<int>
#define pi pair<int, int>
#define rep(i, k, n) for (ll i = (ll)k; i < (ll)n; ++i)
#define per(i, k, n) for (ll i = (ll)n; i >=(ll)k; --i)

using namespace std;

constexpr ll inf = (ll)9e+18;
constexpr ll mod = 1000000007;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll n;
    cin >> n;

    return 0;
}